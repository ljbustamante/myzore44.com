{% extends 'AdminLTE/form_detail.html.twig' %}

{% block page_title %}Catálogo {{ parent.name }}: Edición de Producto {% endblock %}
{% block page_subtitle %}{{ form.vars.data.product|default('Nuevo') }}{% endblock %}
{% block form_content %}
{#{ dump(form.vars.data.image) }}
{% thumbnail form.vars.data.image, 'small' %#}
{{ form_errors(form) }}
{{ form_start(form) }}
	{{ form_row(form.product) }}
	{{ form_row(form.price) }}
    {{ form_row(form.actions) }}
{{ form_end(form) }}
{#{ form(form) }#}
{% endblock %}



